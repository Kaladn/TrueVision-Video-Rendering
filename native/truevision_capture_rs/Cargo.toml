[package]
name = "truevision_capture_rs"
version = "0.1.0"
edition = "2021"

[dependencies]
zip = { version = "0.6", default-features = false, features = ["deflate"] }

[profile.release]
opt-level = 3
lto = "thin"
codegen-units = 1
strip = "symbols"
