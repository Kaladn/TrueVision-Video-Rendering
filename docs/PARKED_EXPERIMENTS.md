# Parked Experiments

Parked means useful, but not part of the stable operator path.

Parked code should be preserved when it carries learning value, but it must not confuse the main product map.

## Browser / YouTube Intake

Files:

```text
scripts/truevision_coordinate_youtube_intake.py
scripts/truevision_youtube_learning_intake_batch.py
truevision_runtime/learning_intake/youtube_cdp.py
truevision_runtime/learning_intake/youtube_metadata.py
```

Status:

```text
parked
operator-approved only
no autonomous browser control
no fresh browser/profile launching
no claim of capture success without receipt
```

Reason:

The useful idea is source intake with receipts. The active product path is local file and local state tooling first.

## Video Generation Proofs

Generated videos under `outputs/` prove that state/render lanes can speak, but they are artifacts, not the product.

Status:

```text
POC locked
not the main system
do not optimize the repo around music-video generation
```

## UI Experiments

Files such as ad hoc HTML panels and mockups are reference material until they are bound to declared actions, policy gates, and receipts.

Rule:

```text
No backend binding = no active control.
No policy gate = no mutation control.
No receipt target = no write control.
```

## Future Workers

New ideas enter as workers:

```text
contract
test
manifest
receipt
operator review
promotion
```

They do not become architecture because they are exciting.
